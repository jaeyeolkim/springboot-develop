spring:
  application:
    name: springboot-develop
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # ??? ?? ?? data.sql ??